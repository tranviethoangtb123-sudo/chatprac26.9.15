# 交接文档（Handover）

这份文档是给"接手这个项目的人 / 另一个 AI 会话"看的。读完它 + 拿到 token，就能完全接管。

## 一、这是什么

**Chat Prac** —— 英语学习网页应用，三个板块：单词 / 句子和对话 / 对话练习。手机竖屏优先，可加到主屏幕并离线使用。

| | |
| --- | --- |
| 线上地址 | <https://tranviethoangtb123-sudo.github.io/chatprac26.9.15/> |
| 代码仓库 | <https://github.com/tranviethoangtb123-sudo/chatprac26.9.15>（public，`main` 分支） |
| 本地目录 | `D:\harness\dsh\program` |
| 托管方式 | GitHub Pages（`build_type=workflow`，由 Actions 发布 `dist/`） |

## 二、接管需要准备什么

1. **整个本地目录** `D:\harness\dsh\program`（含 `.git`）。
2. **GitHub token**：存放在 `.secrets/github-token.txt`（该目录已加入 `.gitignore`，不会入库、不会进部署包）。
   - 需要的权限：**Contents: Read and write**（推代码）、**Actions: Read and write**（触发/重跑部署）、
     **Pages: Read and write**（查 Pages 状态）、**Workflows: Read and write**（改 `.github/workflows/` 下的文件）。
   - 有效期由创建者设定，过期后需要重新生成。
3. **数据缓存** `tools/.cache/`：`ecdict.csv`（63 MB）+ `ipa-en_US.txt`（3 MB）。
   删掉不影响运行，重新生成词库时需要（`node tools/fetch-data.js` 会重新下载）。

## 三、功能结构（两种模式）

| 模式 | 板块 | 说明 |
| --- | --- | --- |
| **查询** | 单词 / 句子和对话 / 对话练习 | 输入框即检索框；没输入时右侧留空 |
| **学习** | 单词 / 对话 | 单词：**列表式背词系统**（词池 = 4198 词大词库 + 162 学习词）；对话：**场景对话库**（12 域 / 60 场景全部收录 / 480 段 4774 话轮，两级选择器：域 + 变体） |

**学习模式的「单词」板块**（和查询模式的数据、进度完全独立）：

- **三个板块**：**今日新词**（每天 50 个新词上限、跨场景域打散；到期的旧词带「复习」标记排最前）/
  **已学习**（按 `learnedAt` 倒序，最新学的在最上面）/ **全部单词**（A-Z 排列）。
- **每词一行**：第一行「英文 + 音标」＋右侧按键（两端对齐），第二行「词性 + 中文」。
  今日新词两个键：**认识**（记忆盒 +1，间隔 1/3/7/14/30 天，满 5 盒已掌握）、**不认识**（归零，10 分钟后再来）；
  已学习只有一个**不认识**键（答对时在复习队列里推进，不用再点）。点单词本身发音（`speechSynthesis`，en-GB，离线）。
- 音标从查询模式的 `data.words.js` 借（`vPhon()`），不重复维护；
  学习词库里不在检索词库中的那 65 个词，音标来自 `assets/js/data.phon.js`
  （由 `node tools/build-phon.js` 从 ipa-dict 生成，会自动跳过检索词库里已有的词）。
- **进度存在 `localStorage` 的 `chatprac-vocab-study`**，键是单词本身（小写）：
  `box / due / done / mastered / learnedAt / custom / day / newToday`。
- **学习模式底部没有输入框**（已按用户要求删掉「快速加词」和对话搜索）；底部改成进度条：
  全场景掌握百分比 + 当前一级标题（域）的掌握百分比，数据来自 `chatprac-dialogue-goal`。
- 学习词库 `assets/js/data.vocab.js` 是竖线格式的字符串表，可手工加词：
  `英文|词性|中文|轨道|域号|子场景|是否拼写陷阱(y/空)|易混提示`
- 已废弃但**保留在代码里**（不再渲染）：收词台、听写轨、语块库、卡片式考核、门禁。它们对应的
  `data.vocab.js` 里的 `chunks`/`trap` 字段也还在。想彻底删掉再说。

- 模式切换按钮在**侧边栏最下方**（`#modeSwitch`），选择存在 `localStorage` 的 `chatprac-mode`。
- 导航项靠 `data-modes="search study"` 属性控制显隐（见 `index.html`）；渲染逻辑在 `app.js` 的
  `renderCurrent()` / `renderWords()`（按 `state.mode` 分支）。
- 深链 `#words` / `#sentences` / `#practice` / `#dialogue` 仍然有效；如果目标板块不属于当前模式，
  `switchTab` 会自动切到对应模式。

### 进度安全（用户在用，改代码前先读这一条）

用户的背词进度和对话掌握记录是**不可再生**的数据，任何改动都必须满足：

1. 不许清空/重建 `chatprac-vocab-study`、`chatprac-dialogue-goal`、`chatprac-mode`、`chatprac-theme`、`chatprac-sync`。
   代码里唯一会删进度的地方是侧栏的「清空全部进度」（`syncReset`）和单词板块的「清空全部进度」按钮，两个都要 `confirm`。
   侧栏的「强制更新」（`syncReload`）只删 Service Worker 缓存（`caches.delete`），**不动 localStorage**。
2. 读写进度时先备份：进度 JSON 解析失败（或格式对不上）时，原文会被复制一份到
   `chatprac-vocab-study.bak` / `chatprac-dialogue-goal.bak`，这样后面任何一次保存都不会把原文彻底冲掉。
   这两个 `.bak` 只做保命用，不参与计数、不进快照。
3. 点「掌握」前会重读一次本机最新进度（`dlgMark` 里先 `dlgGoalLoad()`），
   避免两个页面同时开着时用陈旧副本把新记录冲掉。单词进度没有做这一步（记忆盒状态互相牵连，合并风险更大）。
4. 数据改版后老进度键会变成「孤儿」：计数时按 `dlgValidKeys()` 过滤掉（不然下拉里的数字会比底部进度条虚高），
   但**绝不主动删除**用户数据。
5. `tests/dialogue.js` + `tests/daily.js` + `tests/sync.js` 就是守这几条的，CI 里会跑，改坏就发布不出去。

### 已定稿的产品决定（别再改回去）

下面这些是用户明确要的样子，**不要**当成 bug 或「待优化」去改：

| 决定 | 说明 |
| --- | --- |
| **掌握 / 练习是开关** | 再点一次同一个状态 = 取消（颜色变回、计数 -1）。不要改成「老段再点只更新为今天」。点了会**变色**（掌握变绿 `dlgmark-ok.is-on`、练习变黄 `dlgmark-practice.is-on`），颜色一直保持到再点一次。 |
| **选择器一级标题默认收起** | 点「选择场景」先看到 12 个域标题（手风琴，一次只开一个），不自动展开第一个域。 |
| **不记忆「上次看到哪一段」** | `dlgSel` 只存内存，重开回到「选择场景」。用户明确说不用加。 |
| **朗读按钮已删除** | 美音/英音、整条朗读都不要加回来（用户原话：录音太难听）。单词的点击发音保留。 |
| **学习模式底部没有输入框** | 单词板块底部空着，对话板块底部是那两条进度条。 |
| **对话板块只显示对话本身** | 不显示说话人姓名、不显示统计条 / 目标完成框 / 场景说明。 |
| **词卡不显示例句，没有「模糊」键，单词不加粗** | — |

## 四、部署链路

```
本地改文件 → git push main
   → GitHub Actions (.github/workflows/deploy.yml)
        ① node tests/selfcheck.js    自检
        ② node tests/smoke.js        交互链路测试
        ③ node tools/build-dist.js   打包到 dist/
        ④ upload-pages-artifact → deploy-pages
   → https://tranviethoangtb123-sudo.github.io/chatprac26.9.15/ 更新
```

> 图标（`assets/icons/*.png`）是**提交进仓库的现成文件**，CI 不生成它。
> 生成脚本 `tools/build-icons.ps1` 用的是 Windows 的 GDI+ 字体渲染，只能在 Windows 上跑：
> 白底 + 黑色 "Chat Prac" 两行文字。改图标后记得 `git add assets/icons`。

手动触发部署的两种方式：
- 网页：Actions → 左侧 `Deploy to GitHub Pages` → 右上角 **Run workflow**（workflow 里有 `workflow_dispatch`）。
- API：`POST /repos/{owner}/{repo}/actions/workflows/deploy.yml/dispatches`，body `{"ref":"main"}`。

## 五、改动上线流程（重要）

1. 改文件（内容/界面/词库都行）。
2. **把 `sw.js` 里的 `const VERSION = "vN"` 加一**。
   不加的话，手机端因为 Service Worker 离线缓存，看到的还是旧版本。
3. `git add -A && git commit -m "..." && git push`
4. 等 Actions 跑绿。
5. `node tools/check-live.js` 验证线上（会检查资源 200、manifest、离线缓存版本号、线上词库词数）。

## 六、常用命令

```powershell
# 自检与测试（CI 里按同样顺序全跑一遍，任何一套挂了都不会发布）
node tests/selfcheck.js       # 引用一致性 + 词库/句子/对话数据质量（含音标覆盖、60 场景覆盖）
node tests/smoke.js           # DOM 桩冒烟测试：抽屉、滑动、检索、板块切换、发送、单词/对话板块交互
node tests/daily.js           # 每日任务：一天一批新词、跨天重置、连续天数（会拨假时钟）
node tests/sync.js            # 进度同步：导出 → 清空缓存 → 恢复，单词与对话掌握都要回来
node tests/dialogue.js        # 对话模块：掌握记录关掉再打开还在、跨天只清当天计数、旧进度兼容
node tools/check-live.js      # 部署后自检：线上资源与内容
node tools/check-icons.js     # 图标检查（白底黑字）；加 --art 会打印字符画

# 数据与打包
node tools/fetch-data.js      # 下载 ECDICT + ipa-dict（约 66 MB，缓存在 tools/.cache/）
node tools/build-words.js     # 生成词库（默认 1000 词）
node tools/build-words.js 5000  # 想要更大的词库改这个数字
node tools/build-phon.js      # 重建音标补充表（学习词库里不在检索词库中的词）
& .\tools\build-icons.ps1     # 重新生成主屏幕图标（白底黑字，仅 Windows）
node tools/build-dist.js      # 打包出 dist/（19 个文件，约 1.7 MB）
```

## 七、这台机器上的环境坑（都踩过，别再踩）

在这台 Windows 机器的**受限沙箱**里操作 git / 浏览器时：

1. **TLS 栈**：git 默认用 schannel，会报 `schannel: AcquireCredentialsHandle failed: SEC_E_NO_CREDENTIALS`。
   → 已在本仓库设置 `http.sslBackend = openssl`。
2. **HTTP/2 被重置**：默认协议下 push 报 `Recv failure: Connection was reset`。
   → 已在本仓库设置 `http.version = HTTP/1.1`。
3. **命名管道被禁**：git 的凭据助手（Git Credential Manager）起不来，报
   `sh.exe: *** fatal error - couldn't create signal pipe, Win32 error 5`。
   → 推送时用**带 token 的 URL**，绕开凭据助手（命令见下）。
4. **无头浏览器跑不了**：msedge/firefox 的 headless 会因 mojo named pipe 失败，截图需要在更高权限下进行。
5. **Node 子进程拿不到输出**：`execSync` / `child_process` 的管道被禁。
   → 工具脚本里改成直接读文件（例如 `tools/check-live.js` 直接解析 `.git/config`）。
6. **PowerShell 转义坑**：`node -e "..."` 里带正则表达式很容易被 PowerShell 吃掉。
   → 复杂脚本一律写成文件再执行。

### 推送命令（沙箱内可直接用）

```powershell
$t = (Get-Content .secrets\github-token.txt -Raw).Trim()
git push "https://x-access-token:$t@github.com/tranviethoangtb123-sudo/chatprac26.9.15.git" main
```

（注意：用 URL 推送不会更新本地的 `origin/main` 跟踪引用，推完可以 `git fetch origin` 一次。）

### 如果 github.com 连不上，但 api.github.com 通

这台机器上出现过：`api.github.com` 正常，但 `github.com:443` 连接超时/被重置，`git push` 全部失败。
可以改走 Contents API（`tools/api-sync.js`）：

```powershell
git show --name-status --format= HEAD | Set-Content .secrets\_list.txt -Encoding UTF8
git log -1 --format="%s%n%n%b" | Set-Content .secrets\_msg.txt -Encoding UTF8
node tools/api-sync.js .secrets\_list.txt .secrets\_msg.txt
```

代价：API 提交的哈希和本地不同，本地 `main` 会与远端分叉；
网络恢复后 `git fetch origin && git reset --hard origin/main` 重新对齐即可。

先探测一下再决定用哪条路：

```powershell
node -e "fetch('https://github.com',{headers:{'User-Agent':'x'}}).then(r=>console.log('github.com OK',r.status)).catch(e=>console.log('github.com DOWN',e.cause&&e.cause.message))"
```

## 八、数据来源（授权已确认）

| 数据 | 来源 | 授权 |
| --- | --- | --- |
| 词库释义/词性/词频/考试标签 | [ECDICT](https://github.com/skywind3000/ECDICT) | MIT |
| 词库音标（美式 IPA） | [ipa-dict](https://github.com/open-dict-data/ipa-dict) | MIT |
| 句子库、对话库、练习场景 | 本项目原创编写 | 随本项目 |

## 九、待办与后续方向

- **对话练习接 DeepSeek**：目前是内置模拟回复。接入需要**自己的后端代理**（Cloudflare Workers 等），
  因为网址是公开的，密钥放前端会被人扒走。⚠️ 不要直接把 key 写进 `app.js`。
- 词库可扩容：`node tools/build-words.js 5000`。
- 可选功能：单词/句子朗读（Web Speech API）、生词本、复习提醒、语音识别练口语。
- 打包成 App：页面是纯静态 + PWA，Capacitor / Tauri 可直接包壳。
