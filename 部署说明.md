# 部署说明：让手机随时随地打开 Chat Prac

现在这个应用只在你电脑上（`D:\harness\dsh\program\index.html`）。要让它变成手机上随时能打开的网页，需要**放到一个网站上**。
下面三种都是免费的，选一个就行。**推荐 Cloudflare Pages**：不用装 Git、拖个文件夹上去就好，国内访问也还算稳。

需要上传的文件已经打好了：**`ChatPrac-网站包.zip`**（103 KB），解压后就是 `dist/` 里那 14 个文件。
（`tools/`、`tests/`、`README.md` 这些不用上传，我在打包时已经排除了。）

---

## 方案 A：Cloudflare Pages（推荐）

1. 打开 <https://dash.cloudflare.com/sign-up> 注册一个账号（邮箱即可，免费）。
2. 登录后左侧选 **Workers & Pages** → **Create** → **Pages** → **Upload assets**。
3. 项目名随便起，比如 `chat-prac`（这会成为网址的一部分）。
4. 把 **`ChatPrac-网站包.zip` 直接拖进去**（或拖解压后的 `dist` 文件夹）。
5. 点 **Deploy site**，等十几秒。
6. 拿到网址，形如 `https://chat-prac.pages.dev` —— 这就是你的固定入口，永久有效。

## 方案 B：GitHub Pages

1. 注册 <https://github.com>，新建一个仓库，比如 `chat-prac`，设为 **Public**。
2. 进入仓库 → **Add file** → **Upload files**，把**解压后的 `dist` 里的文件**全部拖进去
   （注意：要传 `index.html`、`manifest.webmanifest`、`sw.js` 和 `assets` 文件夹本身，不要多套一层目录）。
3. 提交后进 **Settings** → 左侧 **Pages** → Source 选 `Deploy from a branch`，
   Branch 选 `main`、目录选 `/ (root)`，保存。
4. 等一两分钟，网址是 `https://你的用户名.github.io/chat-prac/`。

> 提醒：国内访问 GitHub Pages 有时会慢或打不开。介意的话用方案 A。

## 方案 C：Netlify Drop（最快，不用注册也能先试）

1. 打开 <https://app.netlify.com/drop>。
2. 把解压后的 `dist` 文件夹拖进页面。
3. 立刻得到一个网址，形如 `https://随机名字.netlify.app`。
   想固定下来、以后能更新，再注册账号认领这个站点。

---

## 部署完之后：加到手机主屏幕

**iPhone（必须用 Safari 打开网址）**

1. 打开你的网址 → 底部中间的**分享**按钮（方框加箭头）。
2. 往下滑，选 **添加到主屏幕** → 右上角**添加**。
3. 桌面上会出现 Chat Prac 图标，打开后是全屏的，没有浏览器地址栏。

**安卓（Chrome / Edge）**

1. 打开网址 → 右上角 **⋮** 菜单。
2. 选 **安装应用** 或 **添加到主屏幕** → 确认。

**关于离线**：第一次打开需要联网（把内容缓存到手机上，约 290 KB），之后**断网也能用**——
背单词、看句子和对话都没问题，只有"对话练习"接上 DeepSeek 之后才需要联网。
另外首次打开后，随手翻一遍三个板块会缓存得更全。

---

## 以后改了内容怎么更新

1. 改完文件后，**把 `sw.js` 里的 `const VERSION = "v1"` 数字加一**（比如 `v2`）。
   不加的话，手机上因为用了离线缓存，看到的还是旧版本。
2. 重新打包：

   ```powershell
   cd D:\harness\dsh\program
   node tools\build-dist.js
   Compress-Archive -Path "dist\*" -DestinationPath "ChatPrac-网站包.zip" -Force
   ```

3. 把新的 zip 重新上传一次（Cloudflare Pages：进项目 → **Create new deployment** → 拖入）。

手机上如果还是旧版，把应用从主屏幕删掉重新添加，或在浏览器里强制刷新一次。

---

## 一件重要的事：DeepSeek 密钥

现在"对话练习"用的是内置模拟回复，**没有密钥泄露问题**。

但等你接入 DeepSeek 时要注意：**网址是公开的，前端里写的密钥谁都能看到**（右键查看源码就能找到）。
正确做法是自己搭一个小后端（比如 Cloudflare Workers / 一个函数）转发请求，密钥只放在后端。
这件事我们可以等下一步做，到时候我帮你写。
